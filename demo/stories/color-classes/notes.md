There're special color classes to quickly get colorized accordions.

Just add an `aeacc-AetherAccordion--<color>` modifier class to the root `dl` element and you're ready to go!

See HTML below.

((source))
